import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'app_clima_decision_2024_angular_v2',
  webDir: 'www'
};

export default config;
